% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_residue.R
\name{find_residue}
\alias{find_residue}
\title{Search for DNA or AA patterns in sequence data with auto-translation}
\usage{
find_residue(xstring_set, pattern, start_position = NULL, verbose = TRUE)
}
\arguments{
\item{xstring_set}{A \code{DNAStringSet} or \code{AAStringSet} containing sequences.}

\item{pattern}{Character vector of one or more patterns to search for.
Can be nucleotide or amino acid codes.}

\item{start_position}{Optional integer >= 1. If provided and xstring_set
is DNA, translation starts from this position. If NULL (default) and
searching for AA pattern in DNA, auto-detects start position using
detect_sequence_start().}

\item{verbose}{Logical; if TRUE (default), prints diagnostic messages
when auto-translating or detecting start positions.}
}
\value{
A tibble with columns:
\itemize{
\item \code{sequence_name}: character, sequence identifier
\item \code{location}: integer, position of match within the (translated) sequence
\item \code{pattern}: character, the pattern that was matched
}

Rows are in long format (one row per match per pattern per sequence).
Returns empty tibble with correct column structure if no matches found.
}
\description{
Main public function for unified pattern finding across DNAStringSet or
AAStringSet objects. Automatically handles translation when searching for
AA patterns in DNA sequences.
}
\details{
\strong{Pattern Detection Logic:}
\itemize{
\item If xstring_set is \code{AAStringSet}: searches for AA patterns as-is
\item If xstring_set is \code{DNAStringSet} and all patterns are AA:
\itemize{
\item Auto-detects start position (or uses provided start_position)
\item Translates DNA to AA using translate_from_position()
\item Searches translated protein sequences
}
\item If xstring_set is \code{DNAStringSet} and patterns are nucleotides:
\itemize{
\item Searches DNA directly
}
}
}
\examples{
\dontrun{
  dna <- Biostrings::DNAStringSet(c(
    "Seq1" = "ATGAAATAG",
    "Seq2" = "ATGCCCTAA"
  ))
  # Find start codons
  find_residue(dna, pattern = "ATG")

  # Auto-translate and find lysine (K)
  find_residue(dna, pattern = "K")
}

}
