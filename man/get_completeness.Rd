% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_completeness.R
\name{get_completeness}
\alias{get_completeness}
\title{Genome completeness from unambiguous base content}
\usage{
get_completeness(sequences)
}
\arguments{
\item{sequences}{A \link[Biostrings:XStringSet-class]{Biostrings::DNAStringSet}.}
}
\value{
A data.frame with one row per input sequence and two columns:
\code{seq} (the sequence name) and \code{completeness} (numeric fraction in \verb{[0, 1]}).
}
\description{
Computes, per sequence, the fraction of positions that are unambiguous
nucleotides (A/C/G/T) out of the sequence's total length. This is a
reference-free proxy for how complete a consensus genome is: gaps (\code{-}, \code{.})
and ambiguity codes (\code{N}, \code{R}, \code{Y}, ...) count as missing. It lets you derive
a coverage-like quality metric directly from the genome itself, which is
useful when per-sample read coverage (e.g. \verb{samtools coverage}) is
unavailable - for instance for genomes already deposited in a database.
}
\details{
Zero-length sequences return a completeness of 0 (rather than \code{NaN}).
}
\examples{
\dontrun{
ss <- Biostrings::readDNAStringSet("genomes.fasta")
get_completeness(ss)
}

}
