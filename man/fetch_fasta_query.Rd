% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_fasta_query.R
\name{fetch_fasta_query}
\alias{fetch_fasta_query}
\title{Fetch all FASTA sequences matching an NCBI search query}
\usage{
fetch_fasta_query(
  query,
  filename,
  filters = NULL,
  db = "nucleotide",
  batch_size = 500L,
  retmax = Inf,
  verbose = TRUE
)
}
\arguments{
\item{query}{character; base NCBI search term (e.g. \code{"West Nile virus[Organism]"})}

\item{filename}{character; path to output FASTA file (created or overwritten)}

\item{filters}{named character vector of additional NCBI field filters.
Names are field tags; values are the search terms.
e.g. \code{c(Title = "complete genome", PDAT = "2010:2024")}
Each entry is combined with \code{AND} as \code{value[Name]}.}

\item{db}{character; NCBI database (default \code{"nucleotide"})}

\item{batch_size}{integer; sequences per API call (default 500; max 10000)}

\item{retmax}{numeric; maximum total sequences to fetch (default \code{Inf} = all)}

\item{verbose}{logical; print progress messages (default \code{TRUE})}
}
\value{
A \code{DNAStringSet} of the fetched sequences, invisibly. Primary side
effect is writing \code{filename}.
}
\description{
Searches NCBI using a query string plus optional field-tagged filters, then
downloads all matching sequences in batches using NCBI web history. All
results are written to a single FASTA file.
}
\details{
NCBI query syntax: field tags are written as \code{value[Field]}, e.g.
\code{"West Nile virus[Organism]"}. Pass additional constraints via \code{filters}
rather than embedding them in \code{query} directly.

Set the \code{ENTREZ_KEY} environment variable to your NCBI API key to raise
the rate limit from 3 to 10 requests per second.
}
