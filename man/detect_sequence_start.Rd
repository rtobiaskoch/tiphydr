% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_sequence_start.R
\name{detect_sequence_start}
\alias{detect_sequence_start}
\title{Find the modal start position of a pattern across all sequences}
\usage{
detect_sequence_start(xstring_set, pattern = "ATG", verbose = TRUE)
}
\arguments{
\item{xstring_set}{A \code{DNAStringSet} or \code{AAStringSet} containing sequences to
analyze.}

\item{pattern}{A character string representing the pattern to search for.
Default is "ATG" (nucleotide start codon); use "M" for an AAStringSet.}

\item{verbose}{Logical; if TRUE (default), prints a summary message
showing the detected position and counts of sequences with/without pattern.}
}
\value{
A list with two components:
\itemize{
\item \code{position}: integer, the modal (most common) start position
\item \code{table}: table object, frequency table of all positions found
}
}
\description{
Searches for the most common (modal) start position of a pattern
(typically "ATG" start codon) across a DNAStringSet. Useful for
determining the consensus CDS start position before translation.
}
\details{
For each sequence in the DNAStringSet, finds the first occurrence of the
pattern using search_pattern_in_xstring(). Sequences without the pattern
are excluded from the calculation. The modal position is the most frequently
occurring position.

If no sequences contain the pattern, an error is raised.
}
\examples{
\dontrun{
  dna <- Biostrings::DNAStringSet(c(
    "Seq1" = "ATGAAATAG",
    "Seq2" = "ATGCCCTAA"
  ))
  result <- detect_sequence_start(dna, pattern = "ATG", verbose = TRUE)
  # position: 1
}

}
