% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_lineage_colors.R
\name{assign_lineage_colors}
\alias{assign_lineage_colors}
\title{Assign colors to lineage names by family, specificity, and hybrid mixture}
\usage{
assign_lineage_colors(
  muts,
  base_colors = NULL,
  unknown_color = "#BBBBBB",
  ambiguous_color = "#888888"
)
}
\arguments{
\item{muts}{dataframe with columns \code{lineage} (chr), \code{pos} (int),
\code{residue} (chr) — the same lookup table shape passed to
\code{\link[=define_lineage]{define_lineage()}} (e.g. \link{wnv_mut_gff} or \link{wnv_mut_orf}). Row counts per
lineage drive both the partial-lightening fraction and the hybrid blend
weighting.}

\item{base_colors}{dataframe with columns \code{lineage} (chr, the family
name) and \code{color} (chr, a hex color) giving each family's base color at
full saturation. Defaults to \link{wnv_lineage_colors}. Every lineage in
\code{muts} must resolve to exactly one \code{base_colors} family, either by exact
match (a bare family name) or by \code{"<family>_"} prefix (a partial).}

\item{unknown_color, ambiguous_color}{length-1 character; hex colors for
the \code{"unknown"} and \code{"ambiguous"} sentinel labels \code{\link[=define_lineage]{define_lineage()}}
can produce. These are not mutation-based lineages (they have no rows
in \code{muts}), so they're always appended to the output rather than driven
by \code{muts} row counts.}
}
\value{
tibble with columns \code{lineage} (chr) and \code{color} (chr, hex).
}
\description{
Generates a color for every lineage name in \code{muts}, plus dedicated
\code{"unknown"}/\code{"ambiguous"} colors (the sentinel labels \code{\link[=define_lineage]{define_lineage()}}
produces when no or an ambiguous match is found). Colors follow the
lineage-naming convention used throughout this package (see
\code{\link[=define_lineage]{define_lineage()}}): a bare family name (e.g. \code{"NY10"}) gets its base
color at full saturation; a single-family partial (e.g. \code{"NY10_NS2A"})
gets a lighter tint of that family's color, proportional to how many of
the family's required mutations it carries; a hybrid name joining two
pieces with \code{"-"} (e.g. \code{"NY10-SW03_NS5"}) gets a blend of its two
pieces' colors, weighted toward whichever piece requires more mutations.
}
