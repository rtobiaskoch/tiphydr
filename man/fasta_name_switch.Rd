% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fasta_name_switch.R
\name{fasta_name_switch}
\alias{fasta_name_switch}
\title{Swap FASTA sequence names using a metadata lookup}
\usage{
fasta_name_switch(fasta, match_col, replace_col)
}
\arguments{
\item{fasta}{Biostrings::XStringSet (e.g. DNAStringSet) whose \code{names()}
are matched against \code{match_col}.}

\item{match_col}{Character vector of ids matched against \code{names(fasta)}.
A match_col entry matches a fasta name if it occurs
anywhere within it (e.g. "accession" matches
"accession|date|deme"), so exact full-name values still
work as before.}

\item{replace_col}{Character vector of replacement names, same length and
order as \code{match_col}.}
}
\value{
\code{fasta} with \code{names()} swapped where a match was found.
}
\description{
Replaces each name in \code{fasta} with the corresponding entry in \code{replace_col},
matched by position against \code{match_col}. Sequence names with no match in
\code{match_col} are left unchanged (with a warning).
}
