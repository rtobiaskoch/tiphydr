% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fasta_nest.R
\name{fasta_nest}
\alias{fasta_nest}
\title{Nest a DNAStringSet as a list-column in a metadata dataframe}
\usage{
fasta_nest(df, biostring, id_col, delim, verbose = TRUE)
}
\arguments{
\item{df}{dataframe with metadata}

\item{biostring}{DNAStringSet of sequences}

\item{id_col}{string; name of column in df to match against sequence name parts}

\item{delim}{string; delimiter used to split sequence names before matching
(e.g. "|" splits "WNV|2021|CO|CSU123" into c("WNV","2021","CO","CSU123"))}

\item{verbose}{logical; if TRUE prints match summary (default TRUE)}
}
\value{
tibble with all original columns plus a \code{sequence} list-column of
length-1 DNAStringSet objects (NULL where no match found)
}
\description{
Analogous to an sf geometry column: one sequence per metadata row.
The resulting \code{sequence} list-column is compatible with dplyr::filter(),
mutate(), and group_by(). Use fasta_unnest() to reverse.
}
\details{
Matching is done by splitting each sequence name on \code{delim} and checking
whether the row's \code{id_col} value exactly equals any resulting part.
This avoids regex metacharacter issues common in FASTA sequence names.
}
