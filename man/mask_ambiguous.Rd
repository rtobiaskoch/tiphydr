% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask_ambiguous.R
\name{mask_ambiguous}
\alias{mask_ambiguous}
\title{Mask ambiguous characters in a DNA sequence set}
\usage{
mask_ambiguous(seqs, replacement = "N", keep = "ACGT")
}
\arguments{
\item{seqs}{DNAStringSet; sequences to clean.}

\item{replacement}{length-1 character used to replace ambiguous characters
(default "N").}

\item{keep}{character; the set of characters to preserve, given as a single
string (default "ACGT"). Matching is case-insensitive. To preserve
alignment gaps, pass e.g. \code{keep = "ACGT-"}.}
}
\value{
DNAStringSet with ambiguous characters replaced; names preserved.
}
\description{
Replaces every character that is not an unambiguous base (A/C/G/T by default)
with a single replacement character (N by default). This includes IUPAC
ambiguity codes (R, Y, S, W, K, M, B, D, H, V, N) and gap characters
(\code{-}, \code{.}).
}
\details{
Use this instead of a bare \code{gsub()}: calling \code{gsub()} on a DNAStringSet
coerces it to a plain character vector and loses the Biostrings class. This
wrapper does the substitution on the character representation and coerces the
result back to a DNAStringSet, preserving sequence names.
}
