% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_mut_positions.R
\name{resolve_mut_positions}
\alias{resolve_mut_positions}
\title{Resolve gene-relative amino acid positions to absolute genomic codon starts}
\usage{
resolve_mut_positions(muts, gff_path, cds_start = NULL)
}
\arguments{
\item{muts}{data frame with columns: \code{lineage} (chr), \code{gene} (chr),
\code{pos} (int), \code{residue} (chr). \code{pos} is an amino acid position within \code{gene}
(1-indexed) for non-nuc rows, or an absolute nucleotide position for
\code{gene == "nuc"} rows.}

\item{gff_path}{length-1 character; path to a GFF3 file.}

\item{cds_start}{integer (optional); the genomic nucleotide position of the
first base of the CDS (i.e. the A in the polyprotein start ATG), in the
same coordinate space as the GFF. When supplied, an \code{aa_start} column is
computed: the position of the codon in the translated sequence, allowing
direct comparison with \code{\link[=find_residue]{find_residue()}} output.
Formula: \code{ceiling((codon_start - cds_start) / 3 + 1)}.
For WNV genomes with a 96-nt 5' UTR, use \code{cds_start = 97L}.}
}
\value{
tibble with the original four columns plus:
\describe{
\item{comparison_type}{\code{"nuc"} or \code{"aa"}}
\item{codon_start}{integer genomic position of the first nucleotide of
the codon; \code{NA_integer_} for \code{"nuc"} rows}
\item{aa_start}{integer position of the codon in the translated sequence
(counts from 1 at the CDS start); \code{NA_integer_} for \code{"nuc"} rows or
when \code{cds_start} is not supplied}
}
}
\description{
Converts a mutation table that uses gene names and amino acid positions into
one that carries the absolute genomic nucleotide position of each codon. This
is a preprocessing step for \code{\link[=define_lineage]{define_lineage()}}: supply \code{cds_start} to compute
the \code{aa_start} column, then pass it as \code{pos} with \code{mut_type = "aa"}, e.g.
\verb{resolve_mut_positions(muts, gff, cds_start = 97L) |>}
\code{dplyr::mutate(pos = aa_start) |> define_lineage(aln, ref, mut_type = "aa")}.
}
\details{
Rows where \code{gene == "nuc"} are passed through unchanged with
\code{comparison_type = "nuc"} and \code{codon_start = NA}. All other rows are treated
as amino acid positions within the named gene, resolved via the GFF.

Gene matching is case-insensitive substring search against the \verb{product=}
attribute of \code{mature_protein_region_of_CDS} features in the GFF. Products
whose names contain \code{"/"} (frameshift/fusion products such as \code{N-NS4B/WARF4})
are deprioritised so that queries like \code{"NS4B"} resolve to the canonical
protein. An informative error is raised if a query matches zero candidates or
more than one candidate after deprioritisation.
}
