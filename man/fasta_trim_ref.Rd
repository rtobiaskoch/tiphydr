% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fasta_trim_ref.R
\name{fasta_trim_ref}
\alias{fasta_trim_ref}
\title{Align sequences to a reference and trim to reference coordinates}
\usage{
fasta_trim_ref(fasta, ref, alignment = NULL, drop = FALSE, verbose = TRUE)
}
\arguments{
\item{fasta}{DNAStringSet; sequences to align and trim}

\item{ref}{length-1 DNAStringSet; reference sequence defining trim coordinates}

\item{alignment}{optional DNAStringSet; existing alignment to add sequences
into via MAFFT --add profile mode (NULL = build from scratch)}

\item{drop}{logical; if TRUE and alignment provided, remove sequences present
in alignment but absent from fasta (default FALSE)}

\item{verbose}{logical; if TRUE prints progress messages (default TRUE)}
}
\value{
DNAStringSet of trimmed sequences (reference excluded)
}
\description{
Uses MAFFT for alignment. When fasta is the first alignment, all sequences
plus the reference are aligned together with ips::mafft(). When an existing
alignment is provided, new sequences are inserted via MAFFT --add profile
mode, which preserves existing alignment columns exactly.
}
\details{
Coordinate invariant: after trimming, alignment position N equals ungapped
reference position N. This is the contract required by define_lineage().
}
